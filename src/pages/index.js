import React from 'react';
import classnames from 'classnames';
import Layout from '@theme/Layout';
import Link from '@docusaurus/Link';
import useDocusaurusContext from '@docusaurus/useDocusaurusContext';
import useBaseUrl from '@docusaurus/useBaseUrl';
import styles from './styles.module.css';

const features = [
  {
    title: <>Immediate</>,
    imageUrl: 'img/immediate.png',
    description: (
      <>
        LiveData Migrator installs, responds to commands, and migrates your
        data immediately, letting you get started migrating big data without
        risk or complexity.
      </>
    ),
  },
  {
    title: <>Live</>,
    imageUrl: 'img/live.png',
    description: (
      <>
        Migrate your data without disrupting operations in your source environment.
        Only WANdisco's LiveData Platform can migrate your changing data to guarantee
        complete and consistent results.
      </>
    ),
  },
  {
    title: <>Scalable</>,
    imageUrl: 'img/scalable.png',
    description: (
      <>
        Built from the ground up to handle data at petabyte and exabyte scale,
        LiveData Migrator works just as well for enterprise data lakes as it
        does for individual data sets.
      </>
    ),
  },
];

function Feature({imageUrl, title, description}) {
  const imgUrl = useBaseUrl(imageUrl);
  return (
    <div className={classnames('col col--4', styles.feature)}>
      {imgUrl && (
        <div className="text--center">
          <img className={styles.featureImage} src={imgUrl} alt={title} />
        </div>
      )}
      <h3>{title}</h3>
      <p>{description}</p>
    </div>
  );
}

function Home() {
  const context = useDocusaurusContext();
  const {siteConfig = {}} = context;
  return (
    <Layout
      title={`${siteConfig.title}`}
      description="Description will go into a meta tag in <head />">
      <header className={classnames('hero hero--primary', styles.heroBanner)}>
        <div className="container">
          <h1 className="hero__title">{siteConfig.title}</h1>
          <p className="hero__subtitle">{siteConfig.tagline}</p>
          <div className={styles.buttons}>
            <Link
              className={classnames(
                'button button--outline button--secondary button--lg',
                styles.getStarted,
              )}
              to={useBaseUrl('docs/about')}>
              Get Started
            </Link>
          </div>
        </div>
      </header>
      <main>
        {features && features.length > 0 && (
          <section className={styles.features}>
            <div className="container">
              <div className="row">
                {features.map((props, idx) => (
                  <Feature key={idx} {...props} />
                ))}
              </div>
            </div>
          </section>
        )}
      </main>
    </Layout>
  );
}

export default Home;
